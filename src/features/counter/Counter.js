import React, { useState } from 'react';
import { increment } from './CounterSlice';

export function Counter() {
    return (
        <div>
            <button
                onClick={()=>{}}
            >
            +
            </button>
        </div>
    );
}